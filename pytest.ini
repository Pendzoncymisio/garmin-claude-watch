# Run the suite from the repository root: `server/.venv/bin/python -m pytest`.
# pythonpath puts server/ on sys.path so `from app import ...` resolves without
# the package needing to be installed.
[pytest]
testpaths = server/tests
pythonpath = server
